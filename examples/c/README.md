# C Examples

C examples should show module boundaries, header design, opaque structs, resource ownership, and procedural refactoring.

Planned examples:

- global state moved into explicit context
- header exposing internals refactored to opaque type
- long procedural function split by side-effect boundary


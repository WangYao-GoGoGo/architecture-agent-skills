# SQL Examples

SQL examples should show query structure, schema refactoring, indexing decisions, migration plans, and verification with query plans.

Planned examples:

- unstable offset pagination refactored to cursor pagination
- duplicated status columns normalized into an order status history
- slow report query reviewed for indexing and aggregation shape


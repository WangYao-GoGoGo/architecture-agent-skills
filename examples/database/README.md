# Database Examples

Database examples should show access patterns, schema/model decisions, migrations, indexes, cache behavior, and verification notes.

Planned examples:

- cache invalidation review for product detail pages
- relational schema normalization review for orders
- MongoDB embedding vs reference decision
- Redis key design for rate limiting


# Spring Data JPA Skills

Spring Data JPA-specific guidance for repositories, derived queries, entity boundaries, transactions, pagination, and query performance.

Planned skill:

- `spring-data-jpa-repository-review`


# TypeScript Framework Skills

TypeScript framework-specific skills live here when architecture depends on ORM or schema tooling behavior.

Current framework placeholders:

- `prisma`
- `typeorm`
- `sequelize`
- `mongoose`

